# this file is automatically imported by py.test

import sys
import os

# setup the search path to find the sources we want to test
sys.path.insert( 0, os.path.realpath( os.path.dirname( __file__ ) ) )
